class NoteController {
  // DO NOT do the file system yet; need frontend setup to manually test that it works
  // getAll() - returns alphabetically, any sorting can be on frontend
  // getById()
  // getByTitle??? possibly
  // add(note)
  // update(note)
  // delete(note)
}

export default NoteController;
