class NoteRepository {
  // for now, only returning notes in alphabetical order
  // getAll()
  // getById()
  // getByTitle??? possibly
  // add(note)
  // update(note)
  // delete(note)
}

export default NoteRepository;
