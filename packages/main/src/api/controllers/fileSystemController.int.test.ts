// tests with real fs -> all integration!
// returns error if unable to get userData path
// can get real userData path -> well, gotta use linux stuff because that's what the tests will run in
// returns error if unable to get project directory path
// can make all project directory path
// returns error if unable to delete projectdirectories
// can delete all project directories
// returns error if unable to write html file
// can write actual html files
// returns error if unable to readdirSync
// returns error if unable to readFileSync
// can read most recent html file
