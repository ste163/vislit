import { it } from "vitest";

it.skip("If isSubmitting passed in, button is disabled and spinner renders");

it.skip(
  "If isSubmitting is false, button is not disabled and spinner isn't rendered"
);
