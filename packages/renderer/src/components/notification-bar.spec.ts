import { it } from "vitest";

// test rendering error states with colors and icons
// test rendering success states with colors and icons
// test that close button emits close event
// test that it only renders if there's isActive + a message
// test that close emit is called after X amount of seconds
// BUT only when isActive = true
it.skip("based on props, render correct data", () => {});
