{
  "name": "vislit",
  "version": "0.0.1",
  "license": "AGPL-3.0",
  "main": "./src/main/main.ts",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "electron:serve": "vue-tsc --noEmit && vite build | electron ."
  },
  "dependencies": {
    "vue": "^3.2.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.9.2",
    "electron": "^15.0.0",
    "typescript": "^4.4.3",
    "vite": "^2.6.0",
    "vue-tsc": "^0.3.0"
  }
}
